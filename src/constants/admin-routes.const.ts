/**
 * AIT Admin UI route paths (relative to `AIT_BASE_URL`).
 *
 * Prepend with `this.baseURL` (in POMs) or `aitBaseUrl` (in global.setup) to form full URL.
 */
export const ADMIN_ROUTES = {
  BASE: '/admin',
  ROOMS: '/admin/rooms',
  REPORT: '/admin/report',
} as const;
